apiVersion: v1
kind: Service
metadata:
  name: netsuite-api
  namespace: integrations
  labels:
    app: netsuite-api
spec:
  ports:
    - port: 8000
  selector:
    app: netsuite-api
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: netsuite-api
  namespace: integrations
  labels:
    app: netsuite-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: netsuite-api
  strategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: netsuite-api
    spec:
      containers:
        - name: netsuite-api
          image: docker.io/{{DOCKERHUB_USERNAME}}/fyle_netsuite-api:{{RELEASE_VERSION}}
          resources:
            limits:
              memory: 300Mi
            requests:
              memory: 300Mi
          env:
            - name: GET_HOSTS_FROM
              value: dns
          ports:
            - containerPort: 8000
          readinessProbe:
            httpGet:
              path: /api/workspaces/ready/
              port: 8000
            initialDelaySeconds: 10
            timeoutSeconds: 1
          envFrom:
            - secretRef:
                name: netsuite-api-secrets
      imagePullSecrets:
        - name: myregistrykey2
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: netsuite-api-qcluster
  namespace: integrations
  labels:
    app: netsuite-api-qcluster
spec:
  replicas: 1
  selector:
    matchLabels:
      app: netsuite-api-qcluster
  strategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: netsuite-api-qcluster
    spec:
      containers:
        - name: netsuite-api
          image: docker.io/{{DOCKERHUB_USERNAME}}/fyle_netsuite-api:{{RELEASE_VERSION}}
          command: [ '/bin/bash', 'start_qcluster.sh' ]
          resources:
            limits:
              memory: 600Mi
            requests:
              memory: 600Mi
          env:
            - name: GET_HOSTS_FROM
              value: dns
          envFrom:
            - secretRef:
                name: netsuite-api-secrets
      imagePullSecrets:
        - name: myregistrykey2